Libraries to be installed for GUI

pip install dash
pip install dash_core_components
pip install dash_html_components
pip install dash_bootstrap_components
pip install plotly==4.14.3

For executing GUI enter: 
python index.py 
in terminal, go to the link that appears in terminal to launch the app.
e.g. link=http://127.0.0.1:8050/